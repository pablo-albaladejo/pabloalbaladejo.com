# reports/seo

Observatory data. `DASHBOARD.md` and `snapshots/` are generated by
`scripts/geo/*.mjs` — never edit generated files by hand; `queries.json` and
`directory-status.json` are hand-maintained. See `README.md` here.
